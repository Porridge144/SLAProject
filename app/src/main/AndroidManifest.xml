<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gszzz.slaproject">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name_label"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BuildingSelectionForm" />
        <activity android:name=".LevelPlan" />
        <activity android:name=".BuildingDetailForm" />

        <!-- <meta-data -->
        <!-- android:name="com.google.android.geo.API_KEY" -->
        <!-- android:value="AIzaSyDZc0-cIJ66-ojoujwBFsponokxqZJzLYE"/> -->

        <activity android:name=".BasicInfoForm" />
        <activity android:name=".PreInfoForm" />
        <activity android:name=".LevelsForm" />
        <activity android:name=".BuildingDetailForm2" />
        <activity android:name=".server_interaction.LoginForm" />
        <activity android:name=".server_interaction.RegistrationForm"></activity>
    </application>

</manifest>